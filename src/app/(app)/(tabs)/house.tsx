// Route wrapper. The screen lives in src/features so its test file is not
// inside the router tree — anything under src/app is treated as a route.
export { HouseScreen as default } from '@/features/house/HouseScreen';
