/**
 * Setup for the `app` (component) test project only.
 *
 * @testing-library/react-native v12.4+ registers its jest matchers automatically,
 * so there is nothing to import here yet. The `core` project loads no setup file
 * at all — that is the point of the purity boundary. See docs/TESTING.md.
 */

export {};
